# Cleaning Schedule

A tiny static site showing a weekly cleaning schedule with per-task
checkboxes (state saved in the browser, resets each week).

Published automatically via GitHub Pages on every push to `main`.
